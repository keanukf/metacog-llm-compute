# Core (blueprint: vLLM, TextWorld, analysis)
# Pin these to keep RunPod installs reproducible. If you update them on the pod, update here too.
vllm==0.19.1
# Qwen3.5+ checkpoints use model_type qwen3_5 (needs transformers >= 5.2)
transformers==4.56.2
lmstudio>=1.5.0
httpx>=0.27.0
torch==2.4.0
accelerate
textworld
numpy
pandas
scipy
pyyaml
python-dotenv>=1.0.0
matplotlib
seaborn

# Dev / tests
pytest>=7.0.0
pytest-cov>=4.0.0

# Optional: Langfuse observability (or: pip install ".[tracing]" from pyproject)
langfuse>=2.0
