# Optional override for dashboard-only. Prefer using infra/.env for all credentials.
# app.py loads infra/.env first, then this file (override). Copy infra/.env.example to infra/.env.

MLFLOW_TRACKING_URI=http://mlflow.home
MLFLOW_S3_ENDPOINT_URL=http://minio.home
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# MINIO_BUCKET=mlflow
