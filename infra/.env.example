# Copy to .env and set values. Do not commit .env.
# Used by: docker-compose (Portainer stack)

POSTGRES_PASSWORD=changeme_secure_password
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=changeme_minio_password
